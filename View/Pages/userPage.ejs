<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>bsh-<%= user.userName %></title>
    <link rel="stylesheet" href="index.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Barlow:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Figtree:ital,wght@0,300..900;1,300..900&family=Montserrat:ital,wght@0,100..900;1,100..900&family=Playfair+Display:ital,wght@0,400..900;1,400..900&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Raleway:ital,wght@0,100..900;1,100..900&display=swap');
        html{
            overflow-x: hidden;
        }
    </style>
</head>
<body>
    <div
      class="w-screen h-auto py-10 <%= theme.font || '' %>"
      style="
        background-color: <%= theme.backgroundColor || '' %>;
        background: <%= theme.background || '' %>;
        color: <%= theme.textColor || '' %>;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: <%= theme.alignment || 'center' %>;
      "
    >
      <div class="flex flex-col items-center">
        <div class="avatar">
          <img
            src="<%= user.picture || '' %>"
            alt="User Avatar"
            class="w-14 h-14 shadow-md"
            style="border-radius: 50%"
          />
        </div>
        <div class="py-3" style="font-family: <%= theme.font || 'Inter' %>;">
          <%= user ? user.userName : 'user' %>
        </div>
        <div class="py-1" style="font-family: <%= theme.font || 'Inter' %>;">
          <%= user ? user.bio : 'bio' %>
        </div>
        <div
          class="w-full flex flex-col <%= theme.alignment ? 'items-' + theme.alignment : 'items-center' %> py-4"
        >
          <% if (links && links.length > 0) { %>
            <% links.forEach(function(link) { %>
              <div
                class="w-60 lg:w-72 max-w-auto flex flex-wrap justify-center items-center py-2 border"
                style="
                  background-color: <%= theme.buttonColor || '' %>;
                  text-align: center;
                  margin-bottom: 10px;
                  border-radius: <%= theme.buttonStyle || '' %>;
                  font-family: <%= theme.font || 'Inter' %>;
                  color: <%= theme.textColor || '' %>;
                  background: <%= theme.buttonBg || '' %>;
                  border: <%= theme.buttonBorder || '' %>;
                  border-radius: <%= theme.buttonRadius || '' %>;
                "
                onclick="incrementClick()"
              >
                <a href="<%= link.url %>" target="_blank"><%= link.name %></a>
              </div>
            <% }); %>
          <% } else { %>
            <p>No Links</p>
          <% } %>
        </div>
      </div>
    </div>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="index.js"></script>
</body>
</html>
